# Week 1 packages plus Moss semantic memory and PDF search.
fastapi
uvicorn[standard]
openai          # Grok chat via the OpenAI SDK (base_url=https://api.x.ai/v1)
python-dotenv
requests        # xAI's native STT/TTS endpoints (/v1/stt, /v1/tts)
pypdf          # page-level search over course PDFs in ../../srcs
moss>=1.7.1,<2  # local embeddings/search with cloud persistence
